#pragma once
#include "Vector.h"
#include "Matrix.h"

//クロス積
Vector3 Cross(const Vector3& v1, const Vector3& v2);

//bool CrossFrontOrRear(Vector3 cameraPosition, const Vector3& v1, const Vector3& v2);